<nav class="sheet-tabs">
  {{#each tabs as |tab|}}
    <button type="button" class="tab-button{{#if tab.active}} active{{/if}}"
            data-action="selectTab" data-tab="{{tab.id}}">
      <i class="{{tab.icon}}"></i><span>{{localize tab.label}}</span>
    </button>
  {{/each}}
</nav>
